#include "solution.h"

using namespace std;

/** Comment it before submitting
struct Node {
    int val;
    Node* next;
    Node(int val_, Node* next_) {
        val = val_;
        next = next_;
    }
};
**/


Node* Reverse(Node* head, int left, int right) {
    // Your code
    // “ヽ(´▽｀)ノ”
}