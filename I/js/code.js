/** Comment it before submitting
 class Node {
    constructor(value = null, next = null) {
        this.value = value;
        this.next = next;
    }
}
*/

/**
 * @param {Node} head
 * @param {number} left
 * @param {number} right
 * @return {Node}
 */
 var Reverse = function(head, left, right) {
    // Your code
    // “ヽ(´▽｀)ノ”
};