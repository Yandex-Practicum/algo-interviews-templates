#include "solution.h"

/** Comment it before submitting
struct Node {
    int val;
    std::vector<Node*> neighbours;
    Node(int val_) {
        val = val_;
        neighbours = {};
    }
};
**/

Node* cloneGraph(Node* node) {
    // Your code
    // “ヽ(´▽｀)ノ”
}
